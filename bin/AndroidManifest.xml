<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fancyapp.github.travel.sabah"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:name="com.fancyapp.github.travel.sabah.TravelSabahController"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.fancyapp.github.travel.sabah.activity.CategoryListingActivity"
            android:configChanges="orientation|screenSize" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.fancyapp.github.travel.sabah.activity.SubCategoryListingActivity"
            android:configChanges="orientation|screenSize" >
            <intent-filter>
            </intent-filter>
        </activity>
        <activity
            android:name="com.fancyapp.github.travel.sabah.activity.DestinationDetailsActivity"
            android:configChanges="orientation|screenSize" >
            <intent-filter>
            </intent-filter>
        </activity>
        <activity
            android:name="com.fancyapp.github.travel.sabah.activity.DestinationListingActivity"
            android:configChanges="orientation|screenSize" >
            <intent-filter>
            </intent-filter>
        </activity>
    </application>

</manifest>